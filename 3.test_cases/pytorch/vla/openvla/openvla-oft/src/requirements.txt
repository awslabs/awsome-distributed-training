# Python deps for OpenVLA-OFT finetune.py
# The `prismatic` / `experiments` packages are installed from the upstream
# openvla-oft repo via `pip install -e .` in the Dockerfile, which pulls
# most of the heavy ML deps. Anything listed below is an explicit top-up.

draccus==0.8.0
accelerate>=0.30.0
peft>=0.11.0
huggingface-hub>=0.23.0
transformers>=4.40.1
tqdm
wandb
